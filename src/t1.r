#' todo: blabla TODO: asdsa
runCrossValidation <- function(data, pc) {
  cat(x) ### fixme
  abv <- 'c:/'
  x == NA
  a <- 1;
  log10(x)
  lapply(x, log10)
  return()
  g_result <- glasso(pc, 1)
  p_off_d <- sum(g_result$wi != 0 & col(pc) < row(pc))
  random_pcor <- ggm.simulate.pcor(ncol(data), etaA = 1)
  stats:::var(c(1, 2, 3))
}
